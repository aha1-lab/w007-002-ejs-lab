<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Menu</title>
</head>
<body>
    <%- include('./partials/nav') %>
  <h1>Full Menu</h1>

  <h2>Mains</h2>
  <% menu.forEach((meal)=>{%>
    <% if(meal.category == 'mains'){%>
    <li><%=meal.name%> <%= meal.rating== 5 ? "⭐": ""%></li>
    <ul>
      <li>Price: $<%=meal.price%></li>
      <li>Description: <%=meal.details%></li>
      <li>Rating: <%=meal.rating%></li>
    </ul>
  <%}})%>
  <h2>Desserts</h2>
  <% menu.forEach((meal)=>{%>
    <% if(meal.category == 'desserts'){%>
    <li><%=meal.name%> <%= meal.rating== 5 ? "⭐": ""%></li>
    <ul>
      <li>Price: $<%=meal.price%></li>
      <li>Description: <%=meal.details%></li>
      <li>Rating: <%=meal.rating%></li>
    </ul>
  <%}})%>
  <h2>Sides</h2>
  <% menu.forEach((meal)=>{%>
    <% if(meal.category == 'sides'){%>
    <li><%=meal.name%> <%= meal.rating== 5 ? "⭐": ""%></li>
    <ul>
      <li>Price: $<%=meal.price%></li>
      <li>Description: <%=meal.details%></li>
      <li>Rating: <%=meal.rating%></li>
    </ul>
  <%}})%>
</body>
</html>